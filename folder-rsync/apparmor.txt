#include <tunables/global>

profile folderrsync flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  file

  # S6-Overlay
  /init rix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{,**} rwk,
  /run/{s6,s6-rc*,service}/** rix,
  /run/s6/basedir/scripts/** rwix,
  /package/** rix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /etc/s6-overlay/** rix,
  /dev/tty rw

  # Data access
  /data/** rw,
  /config/** r,
  /addons/** r,
  /backup/** r,
  /share/** r,
  /ssl/** r,
  /media/** r,
}